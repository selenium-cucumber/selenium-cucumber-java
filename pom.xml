<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>jonathan-m-foley</groupId>
    <artifactId>selenium-cucumber-java</artifactId>
    <version>0.0.1</version>
    <packaging>jar</packaging>
    <name>selenium-cucumber-java</name>

    <properties>
     <!-- SAPPHIRE Selenium Test Properties -->
        <runSuite>com/harmonia/qa/sapphire/suites/AllTests.class</runSuite>
        <isRemote>false</isRemote>
        <remoteIP>SET_TO_SELENIUM_HUB_HOSTNAME</remoteIP>
        <entrypoint>SET_TO_ENTRY_PAGE_URL</entrypoint>
        <selenium.drivers>BrowserDrivers</selenium.drivers>
        <ieDriverPath>${selenium.drivers}/IEDriverServer.exe</ieDriverPath>
        <chromeDriverPath>${selenium.drivers}/chromedriver.exe</chromeDriverPath>
        <browserChoice>4</browserChoice>
        <implicitWait>0</implicitWait>
        <pageLoadTimeout>30</pageLoadTimeout>
        <dynamicWaitTimeout>10</dynamicWaitTimeout>
        <ajaxCompletionTimeout>10</ajaxCompletionTimeout>
        <ajaxPollingInterval>5</ajaxPollingInterval>
    </properties>

    <dependencies>
        <!-- These are the cucumber dependencies -->
        <dependency>
            <groupId>info.cukes</groupId>
            <artifactId>cucumber-java</artifactId>
            <version>1.2.2</version>
            <scope>test</scope>
        </dependency>

        <!-- These are the cucumber-junit dependencies -->
        <dependency>
            <groupId>info.cukes</groupId>
            <artifactId>cucumber-junit</artifactId>
            <version>1.2.2</version>
            <scope>test</scope>
        </dependency>

        <!-- These are the junit dependencies -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.11</version>
            <scope>test</scope>
        </dependency>
        
    	<!-- These are the selenium-webdriver dependencies -->	
		<dependency>
			<groupId>org.seleniumhq.selenium</groupId>
			<artifactId>selenium-java</artifactId>
			<version>2.45.0</version>
		</dependency>
	
		<dependency>
	        <groupId>org.seleniumhq.selenium</groupId>
	        <artifactId>selenium-server</artifactId>
	        <version>2.53.0</version>
	    </dependency>  

	    <!-- SQA dependencies -->
	    <dependency>
	        <groupId>com.harmonia.qa</groupId>
		    <artifactId>automation-base</artifactId>
		    <version>0.0.12</version>
	    </dependency>

	</dependencies>
    
    <build>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>

        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.2</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                    <source>1.7</source>
                    <target>1.7</target>
                    <compilerArgument>-Werror</compilerArgument>
                    
                    <systemPropertyVariables>
                    	<property>
                    		<name>browser</name>
                    		<value>${browser}</value>
                     	</property>
                     </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>2.5</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
